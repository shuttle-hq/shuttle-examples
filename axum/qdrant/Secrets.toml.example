# When running locally via `cargo shuttle run` these values are ignored.
# Instead Qdrant is run in a local docker container.
#
# Before deploying with `cargo shuttle deploy` you will need to set these values.
# See https://qdrant.tech/documentation/cloud/cloud-quick-start/
# Note that we use gRPC (port 6334) to talk to Qdrant.
CLOUD_URL = 'https://xyz-example.eu-central.aws.cloud.qdrant.io:6334'
API_KEY = 'your_api_key'
